# Golf League App - Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git - it contains secrets!

# ===========================================
# LOCAL DEVELOPMENT
# ===========================================
# For local dev, just set DATABASE_URL to use SQLite file
DATABASE_URL="file:./dev.db"

# ===========================================
# PRODUCTION (Vercel/Turso)
# ===========================================
# Set these in Vercel environment variables, NOT in .env
# TURSO_DATABASE_URL="libsql://your-database.turso.io"
# TURSO_AUTH_TOKEN="your-auth-token-here"

# ===========================================
# SESSION SECURITY (REQUIRED)
# ===========================================
# Used to sign JWT session tokens. Generate with:
#   openssl rand -base64 32
SESSION_SECRET="CHANGE-ME-generate-a-random-secret"

# ===========================================
# BASE URL (Optional)
# ===========================================
# Used for generating absolute URLs (e.g., scorecard links in emails).
# Set this to your production domain in Vercel env vars.
# NEXT_PUBLIC_BASE_URL="https://your-domain.com"

# ===========================================
# SUPER ADMIN
# ===========================================
# Seed the super-admin user with:
#   SUPER_ADMIN_PASSWORD=yourpass npx tsx scripts/seed-superadmin.ts
# SUPER_ADMIN_PASSWORD is only needed during seeding, not at runtime.

# ===========================================
# EMAIL (Optional — Resend)
# ===========================================
# Used to send scorecard links to team captains via email.
# Sign up at https://resend.com to get an API key.
# When not set, email buttons are hidden in the admin UI.
# RESEND_API_KEY="re_your_api_key_here"
# FROM_EMAIL="LeagueLinks <onboarding@resend.dev>"
