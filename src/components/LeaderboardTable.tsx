/**
 * LeaderboardTable — thin wrapper re-exporting TournamentBoard.
 * Keeps the same props interface so existing consumers don't break.
 */
export { TournamentBoard as LeaderboardTable } from "@/components/grounds/TournamentBoard";
